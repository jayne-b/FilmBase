<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.filmbase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconapp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RandomActivity"></activity>
        <activity android:name=".WantInfoEditActivity" />
        <activity android:name=".WantInfoActivity" />
        <activity android:name=".SeenInfoActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".WantActivity" />
        <activity android:name=".SeenActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>